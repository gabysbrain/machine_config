#!/bin/sh

TEMPFILE=`mktemp`
vim ${TEMPFILE}

# make sure something reasonable was entered
if [ -s ${TEMPFILE} ]; then
  echo "\n@worklog" >> ${TEMPFILE}
  jrnl < ${TEMPFILE}
else
  echo "Nothing entered"
fi

rm ${TEMPFILE}

